// engine/src/openai.js
export async function callOpenAI(botName, userMessage, context, session) {
  // TODO: replace with real OpenAI API call
  return `🤖 [${botName}] I heard: "${userMessage}". (fallback reply)`;
}
