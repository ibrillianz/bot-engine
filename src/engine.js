// src/engine.js
// Core bot engine: load config, retrieve context, call OpenAI, manage session
export async function handleMessage(botName, userMessage, sessionId) {
  // TODO: implement
  return "Hello from bot-engine!";
}
